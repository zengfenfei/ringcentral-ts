/* Generated code */
import PathSegment from '../PathSegment';

export default class Settings extends PathSegment {
	constructor(prv: PathSegment, id?: string, service?) {
		super('settings', id, prv, service);
	}
}
