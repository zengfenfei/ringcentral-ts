/* Generated code */
import PathSegment from '../PathSegment';

export default class SecretQuestion extends PathSegment {
	constructor(prv: PathSegment, id?: string, service?) {
		super('secret-question', id, prv, service);
	}
}
